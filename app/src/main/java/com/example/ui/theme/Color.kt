package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Branding (Professional Polish: Teal-600 & Cyan-700)
val PrimaryGreen = Color(0xFF0D9488)       // Teal-600 primary branding
val SecondaryGreen = Color(0xFF0E7490)     // Cyan-700 for secondary indicators / buttons
val PrimaryContainerGreen = Color(0xFFCCFBF1) // Very soft teal-100 container
val OnPrimaryContainerGreen = Color(0xFF115E59) // Deep teal-800 text

val DarkSlateNavy = Color(0xFF0F172A)      // Deep slate-900 for high contrast text & dark mode bg
val SlateSecondary = Color(0xFF64748B)     // Cool slate-500 secondary text
val BackgroundLight = Color(0xFFF8FAFC)    // Modern clean slate-50 background
val CardLowestWhite = Color(0xFFFFFFFF)    // Pure white for bento card backgrounds
val OutlineBorder = Color(0xFFE2E8F0)      // Thin premium slate-200 border color

// Semantic Status Colors
val StatusUrgent = Color(0xFFEF4444)       // Red for urgent debts (< 3 days)
val StatusUrgentContainer = Color(0xFFFEE2E2) // Soft red container
val StatusUpcoming = Color(0xFFD97706)     // Amber-600 for upcoming debts (< 10 days)
val StatusUpcomingContainer = Color(0xFFFEF3C7) // Soft amber container

// Chart Categories (Polish aligned)
val CatSalary = Color(0xFF0D9488)
val CatAlimentation = Color(0xFF0E7490)
val CatTransport = Color(0xFF3B82F6)       // Professional Blue
val CatLoisirs = Color(0xFF8B5CF6)         // Professional Violet
val CatSante = Color(0xFFEF4444)           // Rose/Red
val CatDebt = Color(0xFFF59E0B)            // Amber
val CatAutre = Color(0xFF64748B)           // Slate
